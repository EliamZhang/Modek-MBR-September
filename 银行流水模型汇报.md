# 澳洲模型体系建设 MBR 汇报 · 银行流水模型介绍

### P1｜把原始流水翻译成财务画像：输入与输出

**核心观点**：银行流水是信审最重要的原料，但原料本身没有标签；模型把一笔笔原始交易翻译成收入、负债、支出，直接支撑偿付能力计算与信审决策。

**关键信息**：

- **为什么重要**：澳洲 Responsible Lending 监管下，信审必须核实客户财务状况，核心公式 Serviceability = 收入 − 必要支出 − 负债还款；新客价值模型最重要的特征全部来自流水，老客风险模型里流水子模型占 68.25% 权重——流水翻译的质量，决定上层模型的天花板。
- **输入是什么**：一个申请名下的全部银行流水。每笔交易只有 4 个信息——时间、金额、收支方向、一段文字描述（如 `PAYROLL ACME PTY LTD`、`BILL PAY Fair Go Finance`），没有任何现成的「这是工资 / 这是贷款」标签。
- **输出是什么**：① 每笔交易打上分类标签 + 交易对手（共 35 个类别，如 Wages / SACC Loans / 消费）；② 收入流（月收入估算、预计下次发薪日）；③ 负债流——把外部金融机构的放款与还款交易，聚合成一条条「贷款档案」；④ 分类汇总成客户财务画像，下游据此计算可支配盈余、完成信审。
- **翻译示例**：`PAYROLL ACME PTY LTD` → ACME 公司发放的工资，归入收入流；`BILL PAY Fair Go Finance` → 偿还 Fair Go Finance 的 SACC 贷款，归入负债流。
- **输出示例——收入流**（演示数据，非真实客户）：同一雇主的连续发薪被串成一条收入流——ACME 公司每两周发薪 A$2,600，月收入估算约 A$5,600，预计下次发薪 9 月 11 日。
- **输出示例——负债流，核心能力**（演示数据，非真实客户）：银行流水可以还原客户在外部机构的完整多头负债档案。例如 MoneySpot 给该客户发过 3 笔贷款——A$1,200（3 月，已结清）、A$1,500（6 月，已结清）、A$1,500（7 月，在贷）；在贷这笔按 8 期计划、每期约 A$255，推算含费成本率约 36%，属 SACC 高成本产品档。汇总到客户级：外部机构 3 家、在贷 3 笔（SACC 2 笔 + BNPL 1 笔）、近 90 天新借款 A$2,300。
- **指标体系的丰富性**：基于负债流可以进一步推导——定价档位（从放款与还款节奏推算利率）、多头与负债压力（机构数、借款频率、剩余期数）、还款能力（月还款 / 月收入）、资金饥渴度（近 90 天新增借款）等，全部由流水客观还原，不依赖客户自报。
- **在链路中的位置**：IDA 发起申请 → IDP 调度 → 流水模型（BS-CAT）→ S-CALC 计算可支配盈余 → 风险模型与信审决策。

**建议展示**：「输入 → 翻译 → 输出」三段式页型：左侧原始流水示例行，中间模型图标 +「翻译器」定位；右侧输出区自上而下——收入流卡片（ACME 发薪示例）、负债流「贷款档案」卡片（MoneySpot 3 笔示例：放款 / 还款计划 / 推算利率档位）、派生指标条（多头机构数 / 月还款占收入 / 近 90 天新借款）；演示数据均标注「示例，非真实客户」。

### P2｜模型怎么工作、怎么运维：9 个工位的流水线

**核心观点**：模型是一条 9 个工位组成的流水线，每个工位只认领自己擅长的一类交易；规则写在外置「手册」里，改规则不动代码；上线后靠「人 + AI 助手 + 回归测试」保证质量。

**关键信息**：

- **架构——9 个工位依次过一遍流水**：每笔交易按顺序经过各工位，被最合适的工位认领并打上标签；收入、负债两个工位再把认领结果串成「流」。

```mermaid
flowchart LR
    IN["原始流水"] --> E1["① 商户识别"] --> E2["② 转账"] --> E3["③ 拒付"] --> E4["④ 收入"] --> E5["⑤ 负债"] --> E6["⑥ 其他进账"] --> E7["⑦ 费用"] --> E8["⑧ 房租"] --> E9["⑨ 兜底"] --> OUT["分类结果 + 收入/负债流"]
```

- **通俗解读**：不是黑盒算法——把信审专家的经验写成规则，放在一张张可查看、可修改的 Excel 表格里；推理只查规则、不加载模型权重，普通 CPU 即可运行。
- **知识库就是这些 Excel**：全部规则外置在 24 张规则表中，业务同事可以直接打开修改——最大的「商户库」已收录 87.6 万家澳洲商户，每行是「商户名 + 关键词变体 + 分类」（如 Naked for Satan → 餐饮）；收入表定义工资识别模式（SALARY / PAYROLL 等）；负债表收录贷款机构与产品类型（如 360 Cash Loans → 个人贷款）；房租规则还带置信度评分（如 LANDLORD → 房租，置信度 0.85）。改表即改规则，不用动代码。
- **为什么这样设计**（对比 illion 黑盒分类）：逻辑透明、问题可定位；粒度可控（可拆分 SACC / Non-SACC / BNPL / LOC / EWA 等贷款类型）；口径可配置；Open Banking 等新数据源无分类字段也能直接接入。
- **怎么运维**：「1 人 + 2 个 AI 助手」——质检助手监控分类漂移和异常波动，知识库助手检索新商户并生成更新建议，人确认后发布；每次规则变更必须先过「四层基线回归」（最终结果 / 各工位认领 / 配置指纹 / 汇总指标），任一层有差异即不发布。

**建议展示**：上半页 9 工位横向流程图（规范流程条样式）；下半页左侧「24 张规则表 + 人 + 2 个 AI 助手」运维示意（可放商户库 / 收入表 / 负债表各一行示例），右侧「四层基线回归」检查清单；底部效果数字条可选。
