# PPT 通用格式与排版规范

> **用途**：本文件作为后续所有 PPT 新建、改版、美工优化时的统一设计规范。  
> AI 在处理 PPT 时，应优先读取并遵循本文件，再结合当前 PPT 的实际内容进行排版。  
> 本规范基于现有 PPT 风格整理，并补充了通用页型、信息层级、组件选择、图表规则、易编辑要求及 AI 执行流程。

---

# 0. AI 执行总原则

## 0.1 优先级

AI 修改 PPT 时，按以下优先级执行：

1. **内容完整**
   - 不遗漏任何原始内容、数字、结论、案例、表格字段或必要说明。
   - 不得为了美观擅自删除重要信息。
   - 若内容确实过多，应通过结构重组、层级调整、分页或压缩表达解决，而不是直接删掉。

2. **逻辑清晰**
   - 页面必须有明确的阅读顺序。
   - 优先形成：**结论 → 证据 → 解释**，或 **问题 → 方法 → 结果** 的结构。
   - 避免多个模块平铺但没有主次关系。

3. **风格一致**
   - 优先复用本规范定义的颜色、字体、字号、间距、组件和页型。
   - 同一份 PPT 中相同类型的信息必须使用相同视觉表达。

4. **易编辑**
   - 优先使用 PowerPoint 原生文本框、表格、形状、线条和图表。
   - 避免使用大量碎片化小组件。
   - 避免把一段内容拆成很多独立的小文本框。
   - 能用一个完整模块解决的，不拆成多个零散模块。

5. **管理层阅读效率**
   - 一页只突出 1 个核心结论。
   - 核心数字、关键判断、最终结果必须在 3 秒内可被找到。
   - 细节可以丰富，但不能抢占主视觉。

---

## 0.2 当 AI 不确定如何排版时

不要随机决定。

优先给出 **2–3 个可选布局方案**，例如：

- **方案 A：左结论 + 右证据**
- **方案 B：上结论 + 下表格**
- **方案 C：流程图 + 右侧案例**

并说明每个方案适合的场景，再选择最优方案执行。

---

# 1. 整体视觉风格

## 1.1 风格定位

整体风格：

- 专业
- 简洁
- 管理层汇报
- 数据驱动
- 轻咨询风
- 信息密度较高但不拥挤
- 少装饰、强结构
- 白底为主
- 深蓝 + 金色作为强调色

避免：

- 互联网产品风的大量彩色卡片
- 过度圆角
- 大面积渐变
- 复杂插画
- 大量图标
- 花哨背景
- 过多阴影
- 过多小色块
- 为了“设计感”牺牲信息清晰度

---

## 1.2 页面阅读原则

每页应尽量形成以下视觉顺序：

**页面标题 → 一句话引导 / 核心观点 → 主视觉 → 证据 / 解释 → 结论**

页面中必须有明显的主次关系。

建议控制为：

- 1 个主视觉
- 1–2 个辅助区域
- 1 个结论区

不要同时出现 5–8 个同等权重的小模块。

---

# 2. 页面基础参数

## 2.1 页面尺寸

- 页面尺寸：**13.33" × 7.5"**
- 比例：**16:9**
- 背景：**#FFFFFF**

推荐基础边距：

- 左：0.56"
- 右：0.60"
- 上：0.23"
- 下：约 0.30"–0.40"

---

## 2.2 页面骨架

每页默认采用以下骨架：

### 页面标题

- x：0.66"
- y：0.23"
- 推荐宽度：9.72" 以上
- 字号：20pt
- 字重：加粗
- 颜色：#0A0A0A

### 引导语 / 核心观点

- x：0.56"
- y：0.62"
- 推荐宽度：11.92"
- 字号：18pt
- 常规
- 颜色：#606060

引导语建议为一句完整观点，而不是重复标题。

### 顶部分隔线

- 宽：约 11.89"
- 高：0.01"
- 颜色：#D6D6D6

位于引导语下方。

---

# 3. 栅格与布局系统

## 3.1 默认双栏

适合：

- 结论 + 证据
- 图 + 表
- 方法 + Case
- 结果 + 解释

标准：

- 左栏：x 0.56"–5.40"
- 右栏：x 6.28"–12.70"
- 中间分隔线：x 5.61"
- 分隔线宽：0.01"–0.02"

### 推荐阅读顺序

左栏优先放：

- 核心结论
- 韦恩图
- 大数字
- 统计卡
- 关键公式
- 方法概览

右栏优先放：

- 表格
- Case
- 解释
- 详细证据
- 示例数据

原则：

> **左边负责“看懂”，右边负责“相信”。**

---

## 3.2 上下结构

适合：

- 上方总结，下方表格
- 上方流程，下方案例
- 上方大数字，下方解释
- 数据结论页

建议比例：

- 上部：30%–40%
- 下部：60%–70%

---

## 3.3 三栏结构

只在以下场景使用：

- 3 个并列知识库
- 3 个并列能力模块
- 3 个完全平级指标
- 3 个阶段

要求：

- 三栏尺寸完全一致
- 标题位置一致
- 表格样式一致
- 模块间距一致
- 不允许一栏明显更复杂但仍强行三等分

若第三栏内容明显更多，优先改成 **1+2** 或 **左窄右宽**。

---

# 4. 间距体系

尽量使用稳定的间距体系，避免每页临时决定。

## 4.1 推荐间距

- 小间距：0.08"–0.12"
- 模块内部：0.15"–0.20"
- 模块之间：0.25"–0.35"
- 大区块之间：0.40"–0.55"

原有常用参数：

- 垂直间距：约 0.30"
- 水平间距：约 0.54"

---

## 4.2 对齐规则

所有元素优先遵循：

- 左对齐
- 顶部对齐
- 等宽
- 等高
- 等间距

禁止：

- 肉眼随意摆放
- 相邻模块边缘不齐
- 文本框宽度无规律
- 表格、标题、结论条左右边界不一致

---

# 5. 字号体系

## 5.1 标题层级

| 层级 | 字号 | 字重 | 颜色 |
|---|---:|---|---|
| 页面标题 | 20pt | 加粗 | #0A0A0A |
| 引导语 / 核心观点 | 18pt | 常规 | #606060 |
| 章节标题 | 14pt | 加粗 | #0A0A0A |
| 小节标题 | 12pt | 加粗 | #0A0A0A |
| 模块标题 | 10.5pt | 加粗 | #0A0A0A |
| 正文 | 9pt | 常规 | #3D3C3A |
| 辅助说明 | 8pt | 常规 | #606060 |
| 紧凑表格 | 7–8pt | 常规 | #3D3C3A |

---

## 5.2 数字层级

数字按重要程度分为三级：

### 一级数字

用于：

- 核心 KPI
- 覆盖率
- 一致率
- 总量
- 样本量

格式：

- 15–18pt
- 加粗
- #1F4E79

> **注意（2026-08 第 4 页实测）**：一级数字不得与页面标题同字号（20pt）。曾把指标 98.5% / +317 设为 20pt，与页面标题同级、层级打架；规范上限 18pt，实际取 16pt 与 20pt 标题拉开层级、同时保持醒目。

用于：

- 韦恩图
- 图表主数据
- Case 关键数字

格式：

- 11–12pt
- 加粗
- #0A0A0A 或 #1F4E79

### 三级数字

用于：

- 百分比说明
- 辅助指标
- 注释

格式：

- 8–9pt
- 常规
- #3D3C3A / #606060

---

# 6. 色彩系统

## 6.1 主色

### 深蓝

`#1F4E79`

用途：

- 核心数字
- 编号
- Case 标签
- 重点线条
- 关键模块标题
- 韦恩图左侧

---

## 6.2 辅色

### 金色

`#D6A000`

用途：

- 章节小竖条
- 胶囊标签
- 结论强调
- 次级对比
- 韦恩图右侧

---

## 6.3 浅色背景

| 用途 | 颜色 |
|---|---|
| 浅蓝卡片 | #E3EBF4 |
| 浅金结论条 | #FAF3E0 |
| 流程底带 / 浅灰背景 | #F0F0F0 |
| 表头备用浅灰 | #F2F2F2 |

---

## 6.4 文字颜色

- 标题：#0A0A0A
- 正文：#3D3C3A
- 次要说明：#606060
- 弱化线条：#D6D6D6

---

## 6.5 色彩使用原则

一页尽量只使用：

- 黑 / 深灰
- 深蓝
- 金色
- 1 种浅底色

避免同时出现多种彩色背景。

强调优先通过：

1. 字号
2. 粗细
3. 位置
4. 留白
5. 最后才是颜色

---

# 7. 章节标题组件

章节标题统一使用：

- 左侧金色竖条
- 右侧标题

参数：

- 金色竖条：0.06"–0.08" 宽
- 高：0.18"–0.25"
- 颜色：#D6A000
- 标题：12–14pt 加粗
- 标题颜色：#0A0A0A

禁止用大面积色块作为章节标题背景。

---

# 8. 表格规范

## 8.1 基础表格风格

默认使用极简横线表。

### 边框

- 不设置左右边框
- 不使用完整网格
- 表头底部：1.5pt 黑线 #0A0A0A
- 数据行底部：0.75pt #D6D6D6
- 必要时数据行顶部使用 1.5pt 黑线做分组

### 填充

默认：

- 无填充
- 透明背景
- 不使用深色表头

特殊情况可使用：

- #F2F2F2 浅灰表头
- #E3EBF4 浅蓝表头：用于双栏页**平衡右侧蓝色元素**（卡片 / 色块）时，与右侧同色系呼应
- 但同一页不要混用两种表格风格（浅灰表头 + 浅蓝表头不能同时出现）

---

## 8.2 字体

- 表头：8pt 加粗
- 数据：8pt 常规
- 紧凑辅助表：7pt
- 字体统一使用微软雅黑 / Microsoft YaHei

---

## 8.3 行高

- 表头：0.19"–0.20"
- 普通数据行：0.19"–0.23"
- 内容较多：最高约 0.31"

---

## 8.4 列宽

根据内容自动分配。

参考：

- 全宽表：约 11.92"
- 半栏表：5.71"–6.06"

原则：

- 文本列宽
- 数字列窄
- 百分比列窄
- 不要让短数字列占过多宽度
- 不要为了等宽牺牲内容可读性

---

## 8.5 表格与文字的结合

不要出现：

> 一大段文字放在表格旁边，彼此没有对应关系。

优先使用以下方式：

### 方式 A：表格 + 行内高亮

直接在表格中突出关键数据。

### 方式 B：表格 + 下方解读

表格下方只保留 1–3 条关键结论。

### 方式 C：表格 + 右侧 Case

表格负责量化结果，Case 负责解释为什么。

### 方式 D：表格 + 统计卡

先用统计卡告诉读者结论，再用表格做证据。

---

# 9. 统计卡

适用于：

- Coverage
- Agreement
- Accuracy
- Sample Size
- Volume
- Improvement

标准参数：

- 尺寸：约 1.42" × 1.00"
- 背景：#E3EBF4
- 顶部蓝条：0.06" #1F4E79

内容顺序：

1. 标题：9pt 加粗
2. 核心数值：15pt 加粗 #1F4E79
3. 副说明：8pt #3D3C3A

### 使用限制

一页推荐：

- 2–4 个统计卡

不建议：

- 6 个以上
- 每个指标都做卡片
- 同时出现不同尺寸的卡片

---

# 10. 结论条

用于：

- 页面最终结论
- 表格解读
- Case 结论
- 风险提醒

样式：

- 浅金背景：#FAF3E0
- 左侧金色竖条：0.07" #D6A000
- 高度：0.30"–0.50"
- 字号：8.5–9.5pt
- 加粗
- 颜色：#0A0A0A

原则：

- 每个结论条只说一个观点
- 尽量控制在两行以内
- 不要把完整段落放进结论条

---

# 11. Case 模块

适用于解释：

- 模型差异
- 错误案例
- 覆盖新增
- 规则逻辑
- 识别原因

标准结构：

**Case 标签 → 标题 → 简短描述 → 结论**

### Case 标签

- 0.62" × 0.26"
- #1F4E79
- 白字
- 8.5pt 加粗

### Case 标题

- 10.5pt
- 加粗
- #0A0A0A

### 描述

- 8.5–9pt
- #3D3C3A

### 结尾

优先使用结论条，不要继续追加长段落。

---

# 12. 韦恩图

适用于：

- 两个模型覆盖关系
- 新旧模型交集
- 数据源重合
- 用户群交集

参数：

- 两圆直径：2.35"（需要突出大圆独有区时不等大：大圆 2.82"、小圆 2.72"）
- 圆心距：约 1.30"（重叠需要更大时收窄到 1.16"）
- 重叠：约 1.05"；一致率高时加大到 1.60" 以上（本例 92% 一致率 → 重叠 1.61"）

左圆：

- #1F4E79
- alpha 约 28%

右圆：

- #D6A000
- alpha 约 28%

边框：

- 1.2pt 同色描边

数字：

- 独有区：11pt 加粗
- 交集：12pt 加粗
- 副说明：7.5pt

圆上方必须直接写：

**名称 + 数量 + 覆盖率**

不要依赖单独图例。

**等大圆独有区相等**：等大圆（同直径）时，左独有区 = 右独有区 = 圆心距。要体现「某侧抓到更多」（独有区更大），必须让该侧圆**更大**（不等大），仅调整圆心距无法改变两侧独有区之比。

---

# 13. 胶囊标签

适用于：

- 知识库名称
- 模块名称
- 数据源名称

标准：

- 约 1.42" × 0.25"
- 背景：#D6A000
- 白字
- 10–10.5pt 加粗

限制：

- 同一页最多 3–4 个
- 只用于标签，不用于整段正文

---

# 14. 编号块

适用于：

- 1 / 2 / 3 / 4 步骤
- 关键能力
- 业务问题

### 圆形编号

- 直径：0.34"
- #1F4E79
- 白字
- 10pt 加粗

### 方形编号

- 0.34" × 0.34"
- #1F4E79
- 白字
- 10pt 加粗

### 金色徽章

- 0.30" × 0.30"
- #D6A000
- 白字
- 12pt 加粗

---

# 15. 流程组件

## 15.1 简单流程条

适用于：

- 模型流程
- 数据链路
- 引擎顺序

背景：

- #F0F0F0
- 全宽约 11.89"

推荐高度：

- 0.30"–0.55"

文字：

- 11–12pt
- 加粗

示例：

`Init → Income → Liability → Other Engines → Quality Review`

要求：

- 流程方向保持从左到右
- 不要混用上下箭头
- 步骤超过 6 个时，应分阶段或换成两层结构

---

## 15.2 闭环流程

适合：

**监控 → 定位 → 输出 → 更新**

建议用：

- 4 列表格
- 或 4 个等宽模块

不要使用复杂环形 SmartArt。

---

# 16. 公式模块

适用于：

- Serviceability
- 业务公式
- 指标拆解

推荐样式：

- 浅灰底 #F0F0F0
- 左侧黑色竖条 0.06"
- 小标题：10.5pt 加粗 #3D3C3A
- 核心公式：15pt 加粗

示例：

**Serviceability = Income − Essential Expenses − Debt Repayments**

公式必须突出，但不要加复杂数学装饰。

---

# 17. 图表规范

## 17.1 图表使用原则

只有当图比表更快表达结论时才使用图表。

优先级：

1. 韦恩图：交集 / 覆盖
2. 条形图：类别对比
3. 堆叠条形图：组成关系
4. 折线图：趋势
5. 散点图：相关性
6. 表格：精确数字

避免：

- 饼图
- 3D 图表
- 雷达图
- 仪表盘
- 复杂组合图

除非业务场景非常明确。

---

## 17.2 条形图

推荐：

- 横向条形图优先
- 类别名称放左侧
- 数值直接显示
- 尽量不依赖图例
- 网格线弱化或删除

颜色：

- 主系列：#1F4E79
- 对比系列：#D6A000
- 非重点系列：浅灰

---

## 17.3 堆叠条形图

适合：

- Finv 独有 / 共认 / Illion 独有
- 分类组成
- 结果构成

如果核心目标是体现“交集关系”，优先韦恩图。

如果核心目标是体现“总量构成”，优先堆叠条形图。

---

# 18. 信息密度控制

## 18.1 一页最多几个重点

建议：

- 1 个核心结论
- 2–4 个关键数字
- 1 个主要图 / 表
- 2–3 条解释

如果超过：

- 2 张复杂表格
- 3 个大段正文
- 6 个卡片
- 8 个并列模块

通常说明页面需要重构。

---

## 18.2 长文字处理

长段落优先改为：

### 方式 A

**加粗结论 + 一句话解释**

### 方式 B

3 条以内 bullet

### 方式 C

表格两列：

| 问题 | 说明 |
|---|---|

### 方式 D

Case 模块

不要单纯把字号缩小到 6–7pt。

---

# 19. PPT 常用页型

AI 应先判断页面属于哪种页型，再开始排版。

---

## 19.1 页型 A：核心观点页

适用：

- 高层结论
- 战略观点
- 核心判断

结构：

**标题 → 一句话观点 → 1 个主视觉 → 2–3 个证据**

不要堆大表。

---

## 19.2 页型 B：流程 / 能力页

适用：

- 产品能力
- 模型流程
- 数据处理链路

结构：

**标题 → 流程条 → 关键模块 → 结果**

优先横向阅读。

---

## 19.3 页型 C：输入 → 处理 → 输出

适用：

- 银行流水
- 模型识别
- 数据加工

结构：

左：原始输入  
中：模型 / 引擎  
右：结构化输出

底部可增加最终业务价值。

---

## 19.4 页型 D：对比页

适用：

- 新旧模型
- Finv vs Illion
- 模型 vs Fiskil
- Before / After

优先结构：

左：结论 / 韦恩图  
右：矩阵 / 表格 / Case

避免左右两边都放大段文字。

---

## 19.5 页型 E：表格证据页

适用：

- QA
- OOT
- 测试结果
- 字段说明

结构：

上：2–4 个 KPI  
中：主表格  
下：1–2 条结论

---

## 19.6 页型 F：知识库页

适用：

- 收入知识库
- 贷款知识库
- 澳洲企业库

结构：

3 个平行模块。

每个模块：

**胶囊标签 → 表格 → 一句说明**

---

## 19.7 页型 G：Case 页

适用：

- 典型案例
- 错误分析
- 模型差异解释

结构：

左：总体结论 / 数据  
右：2–3 个 Case

Case 样式必须完全一致。

---

## 19.8 页型 H：总结页

结构：

- 3 个核心结论
- 1 个下一步
- 可配 3 个数字

不要重新堆大量证据。

---

# 20. 特殊组件库

## 20.1 键值窄表

适合单笔流水字段展示：

| 字段 | 内容 |
|---|---|
| Date | ... |
| Amount | ... |
| Direction | ... |
| Description | ... |

列宽参考：

- 左：约 1.3"
- 右：约 4.2"

---

## 20.2 底部结论带

适合：

- 指标公式
- 最终业务价值
- 模型输出结论

结构：

**金色标签 + 核心公式 / 结论 + 灰色注释**

全宽约 11.89"。

---

## 20.3 竖排侧标签

样式：

- 浅蓝 #E3EBF4
- 左侧蓝条 #1F4E79
- 竖排文字

适合：

- 用户维度
- 单笔借款维度
- Input / Output 分组

仅在确实需要分组时使用。

---

# 21. 内容表达规则

## 21.1 标题不是描述，要表达结论

不推荐：

> 模型对比结果

推荐：

> BS-CAT 在保持高一致率的同时识别出更多有效贷款交易

---

## 21.2 引导语负责解释“为什么重要”

不重复标题。

不推荐：

> 以下展示模型对比结果。

推荐：

> OOT 测试显示，新增覆盖主要来自 BNPL、EWA 与 Home Loan，同时共认交易保持较高一致性。

---

## 21.3 Bullet 规则

- 一组最多 3–5 条
- 每条尽量不超过两行
- 句式统一
- 关键词可加粗
- 不在每条 bullet 前再加大量图标

---

# 22. 文本与表格结合规则

AI 必须检查以下问题：

### 问题 1

文字是否只是重复表格？

如果是，删除重复文字，只保留结论。

### 问题 2

文字和表格是否没有视觉关联？

如果是：

- 把文字放到表格下方形成解读
- 或变成 Case
- 或变成结论条

### 问题 3

表格信息太复杂？

如果是：

- 只保留最重要字段
- 或拆分成主表 + 解释
- 不要直接把 12 列表格缩小

---

# 23. 易编辑规范

这是强制要求。

## 23.1 文本

同一逻辑模块尽量使用一个文本框。

禁止：

- 一句话拆成多个文本框
- 一个 bullet 一个独立文本框
- 单个数字单独做成无法关联的文本框

---

## 23.2 组件

相关元素应组合：

- Case 标签 + 标题 + 描述
- 统计卡
- 编号 + 标题 + 正文
- 韦恩图
- 流程步骤

方便整体移动。

---

## 23.3 图表

优先使用 PowerPoint 原生图表。

不要把可编辑图表转成图片。

---

## 23.4 表格

Markdown 表格必须转换成 PowerPoint 真实表格。

禁止：

- Markdown 文本直接贴在 PPT
- 用很多文本框模拟表格
- 把表格截图放进 PPT

---

# 24. 禁止事项

AI 不应：

- 自动增加无意义图标
- 每个模块都加背景色
- 使用大量圆角矩形
- 使用 5 种以上强调色
- 做复杂阴影
- 做渐变背景
- 加无意义装饰线
- 把一页拆成十几个小色块
- 用图片代替可编辑表格
- 为追求整齐强制所有列等宽
- 为塞内容把正文字号压到 7pt 以下
- 擅自改变原始数字
- 擅自改变结论含义
- 擅自增加未经验证的数据

---

# 25. 页面优化决策规则

AI 优化已有页面时，按以下顺序判断。

## Step 1：先看内容

确认：

- 页面要表达的唯一核心结论是什么？
- 哪些是证据？
- 哪些是解释？
- 哪些只是辅助信息？

---

## Step 2：判断页面问题

常见问题：

- 文字太多
- 主次不清
- 表格与文字割裂
- 卡片太碎
- 左右不平衡
- 结论不突出
- 重复信息
- 同级元素样式不统一

---

## Step 3：选择页型

从第 19 节选择最适合的页面类型。

---

## Step 4：选择主视觉

优先选择：

- 韦恩图
- 大数字
- 流程
- 主表
- 对比条形图
- Case

一页只设一个主视觉。

---

## Step 5：安排证据

证据放在：

- 主视觉右侧
- 主视觉下方
- Case 区域

不要和主视觉争抢面积。

---

## Step 6：增加结论

在页面下方或 Case 末尾加入简洁结论条。

---

# 26. AI 修改已有 PPT 的执行流程

当用户提供原始 PPT 时：

1. **先解析原 PPT**
   - 页面尺寸
   - 字体
   - 颜色
   - 常用组件
   - 表格风格
   - 标题位置
   - 页边距
   - 图表类型

2. **优先复用原有组件**
   - 不重新创造完全不同的设计语言。

3. **检查每一页**
   - 核心结论是否清晰
   - 布局是否合理
   - 内容是否完整
   - 组件是否统一
   - 是否存在文字与表格割裂
   - 是否存在大量碎片化色块

4. **重新排版**
   - 优先移动、组合、对齐和调整层级。
   - 不要一上来就换整套风格。

5. **最终检查**
   - 所有文字完整
   - 所有表格完整
   - 数字未修改
   - 页面没有溢出
   - 字号可读
   - 元素可编辑
   - 跨页风格一致

---

# 27. 页面质量检查清单

每一页完成后检查：

## 内容

- [ ] 所有原始内容是否保留？
- [ ] 是否存在未经确认的新数字？
- [ ] 是否有重复表达？
- [ ] 是否只有一个主要结论？

## 结构

- [ ] 3 秒内能否找到页面重点？
- [ ] 主视觉是否明确？
- [ ] 阅读顺序是否自然？
- [ ] 证据是否紧跟结论？

## 排版

- [ ] 左右边界是否对齐？
- [ ] 模块间距是否统一？
- [ ] 相同组件尺寸是否一致？
- [ ] 是否存在孤立的小文本框？
- [ ] 页面是否过于拥挤？

## 字体

- [ ] 标题 20pt 左右？
- [ ] 正文一般不低于 8–9pt？
- [ ] 重要数字是否足够突出？

## 颜色

- [ ] 是否以深蓝 + 金色为主？
- [ ] 是否出现过多颜色？
- [ ] 是否通过颜色之外的方式建立层级？

## 编辑性

- [ ] 表格是否为真实表格？
- [ ] 图表是否可编辑？
- [ ] 相关元素是否已组合？
- [ ] 是否避免大量碎片化组件？
- [ ] 左右视觉重量是否平衡（见第 37 节）？

---

# 28. 快速组件选择表

| 内容类型 | 优先组件 |
|---|---|
| 2 个模型覆盖关系 | 韦恩图 |
| 2 个模型结果构成 | 堆叠条形图 |
| 3–4 个核心 KPI | 统计卡 |
| 复杂结果数字 | 表格 |
| 典型差异解释 | Case |
| 一句话结论 | 结论条 |
| 流程 / 引擎 | 流程条 |
| 4 步闭环 | 四列表格 / 等宽模块 |
| 3–4 个步骤说明 | 步骤卡片（第 35 节） |
| 单笔流水字段 | 键值窄表 |
| 3 个知识库 | 三栏知识库 |
| 原始数据到结果 | Input → Engine → Output |
| 公式 | 灰底公式条 |
| 重要章节 | 金色竖条 + 标题 |

---

# 29. 默认推荐版式

如果没有特殊要求，优先使用以下模板。

## 模板 1：左结论 + 右证据

左：

- 章节标题
- 韦恩图 / KPI / 公式
- 结论条

右：

- 表格 / Case
- 数据解释

适合：模型评估、对比、结果解释。

---

## 模板 2：上结论 + 下表格

上：

- 3 个 KPI
- 一句话结论

下：

- 全宽表格
- 1 条结论

适合：OOT、QA、监控结果。

---

## 模板 3：流程 + 结果

上：

- 流程条

中：

- 3–4 个能力模块

下：

- 输出 / 业务价值

适合：产品能力、模型流程。

---

## 模板 4：三栏知识库

每栏：

- 金色胶囊标签
- 表格
- 简短说明

适合：知识库、数据源、能力模块。

---

# 30. 组件统一原则

同页多个相同组件必须做到：

- 宽度一致
- 高度一致
- 标题字号一致
- 正文字号一致
- 内边距一致
- 标题位置一致
- 色彩一致

跨页同类组件也应尽量一致。

例如：

- 所有 Case 模块保持同一种格式
- 所有统计卡保持同一种格式
- 所有表格保持同一种边框逻辑
- 所有章节标题都使用金色竖条

---

# 31. 关于“设计感”的判断标准

设计感不等于增加元素。

本规范中的设计感主要来自：

- 对齐
- 留白
- 层级
- 大小关系
- 统一
- 内容结构
- 少量稳定的强调色

当一个页面已经清晰时，不需要额外增加装饰。

---

# 32. 最终输出要求

AI 完成 PPT 后必须保证：

1. 内容完整。
2. 数字准确。
3. 逻辑顺序清晰。
4. 视觉重点明确。
5. 风格与本规范一致。
6. 表格是真实表格。
7. 图表尽量可编辑。
8. 不使用大量碎片化小组件。
9. 同类模块统一。
10. 不因为美工修改原意。
11. 页面留白合理。
12. 正文字号保持可读。
13. 每页都有明显的核心信息。
14. 如果原页面结构明显不合理，可以重新布局，但必须保留内容。
15. 当无法确定最佳布局时，先提供 2–3 个方案，再选择或等待用户决定。

---

# 33. 一句话执行标准

> **用最少的组件，把页面的核心结论讲清楚；用统一的表格、图形和 Case 提供证据；保持深蓝 + 金色、白底、专业、易编辑，不做碎片化装饰。**

---

# 34. PPTX 编辑实战经验（踩坑记录）

> 以下经验来自 V1.7 第 2 页修改实战（2026-08）。用 python-pptx 或直接改 XML 时，PowerPoint 的渲染行为与直觉不一致，按本节检查可避免返工。

## 34.1 中文文字宽度的"隐形框"问题

**现象**：文本框明明设了宽 12"，但 PowerPoint 渲染时文字只排到约 3.5" 就折行，或干脆单行溢出到页面外。

**根因**（按排查顺序排查）：

1. **run 的 rPr 缺少东亚字体声明**。python-pptx 写入 run 时只写 `latin typeface`，PowerPoint 渲染中文需要 `<a:ea typeface="微软雅黑"/>`。缺 `ea` 时中文按默认等宽字体渲染，每字宽从 0.275" 变成 0.32"+，直接超框溢出。
2. **段落 pPr 的 `latinLnBrk="0"`**（禁止拉丁换行）会让整段文字不换行、单行溢出。
3. **版式（layout）上的装饰元素覆盖标题区域**。layout 里一个"空文本框"横在标题文字上方时，PowerPoint 会把标题的排版宽度限制在该文本框范围内，无论标题框本身多宽。

**修复方法（按可靠性排序）**：

- **首选：用 PowerPoint COM 写入文字**（`TextRange.Text = ...`），COM 会自动生成 PowerPoint 认可的完整 XML（含 `ea` 字体、`latinLnBrk="1"`、`lang="zh-CN"` 等）。实测 python-pptx 写入后渲染溢出的文字，用 COM 重写同一句后就正常。
- 次选：手动补 XML，至少保证：rPr 含 `lang="zh-CN"` + `<a:ea typeface="微软雅黑"/>`；pPr 含 `latinLnBrk="1"`。
- 排查顺序：先确认是"文字渲染宽度异常"（量每字宽是否 ≈0.275"@20pt），再查 rPr 的 `ea` 字体，再查 pPr 的 `latinLnBrk`，最后查 layout 遮挡。

## 34.2 渲染验证的坑

- **AI 无识图工具，禁止用 Read 查看渲染 PNG**（浪费大量时间且无收益）。验证一律用 **PowerPoint COM 读结构化属性**：形状位置/尺寸、`Table.Rows(i).Height`（真实渲染行高）、`TextRange.Text`/`Font.Color`/`Font.Size`、`Fill.ForeColor.RGB`——比像素检测快且准确。
- **PowerPoint COM 的 `Export` 仍是权威渲染器**，`python-pptx` 读 XML 只能看属性、看不到真实排版效果；但无需导出 PNG 人眼看，直接读 COM 属性即可（行高是否被内容撑高、文字是否折行，COM 的 `Rows.Height` / `TextRange` 都有答案）。
- **中文抗锯齿后偏灰**：9pt 文字在浅金底上渲染后平均亮度可达 150+，检测阈值要放宽（<200），并限定在目标区域内（如金底内）查找，不要全图扫描。
- **检测"折行"别只看行数**：12pt 文字每字宽仅 0.17"，21 个字单行才 3.6"，若误用 20pt 的 0.275"/字估算，会把正常的单行误判成"折行"。

## 34.3 表格与结论条的重叠

**现象**：新增的结论条放到了表格上面，两者完全重叠。

**原因**：表格的实际 y 位置（`shape.top` + 行高累计）与预估不一致——表格行高 0.23" 只是默认值，实际被内容撑高。改前必须用 COM 或读 XML 量出表格**真实底部**（top + 各行高之和），再定位结论条。

**原则**：任何"放在表格下方"的元素，间距 = 表格真实底部 − 元素顶部，至少留 0.05"–0.1" 间隙；不够就把表格上移或压缩行高，而不是硬塞。

## 34.4 竖排文字的坑

- 竖排文字用 `bodyPr vert="eaVert"`，文字会**垂直从底部向上**排布，且 shape 的高度要足够容纳文字长度（宽度很窄时，文字按行竖排）。
- 多个标签共享同一套竖排样式时，**别用同一个 x**——两个标签位置必须各自绑定到对应的表/栏，否则会叠在一起只显示一个。
- 竖排标签内部文字若用 `word_wrap=False`，横向空间不足时文字会溢出，需实测确认。

## 34.5 改 layout 影响所有页面

- layout 是**所有用该版式的页面共用**的。改 layout（移动、删除元素）会同时影响所有页，改前必须确认其他页是否依赖该元素。
- 排查"哪个元素在限制标题排版"时，可把疑点元素移到页面外（y=10"）测试，确认影响面后再决定是否真的删。

## 34.6 修改流程建议（避免返工）

1. **改前先备份**原文件（或单独存一份）。
2. **用 COM 或 XML 量清所有相关元素的实际位置**（top/width/行高），不要用记忆值或估算值。
3. 文本写入**优先 COM**（`TextRange.Text`），不要直接拼 XML。
4. 改完**用 COM 读结构化属性验证**（行高是否被撑高、文字是否折行、颜色/字号是否生效），确认无重叠、无溢出、文字完整。
5. 最后检查**所有页**（layout 改动影响面）无溢出、无内容缺失。

## 34.7 已知遗留问题（供后续处理）

- 原 PPT 第 1/3/4 页标题与第 2 页有同样的 `latinLnBrk`/`ea` 字体隐患，可能在其他机器上溢出，建议按 34.1 统一修复。
- 第 3 页 `liab-table` 溢出页面底部约 0.08"，需压缩行高或上移。

## 34.8 表格"无边框"必须显式声明，不能靠默认样式

**现象**：移除表格 `tableStyleId` 后，同一页的左右两张表出现样式差异——左边是极简横线表（无左右边框），右边表格四周出现黑色外边框。

**根因**：`tableStyleId` 移除后，PowerPoint 会按**主题默认表样式**渲染表格。主题默认样式自带表格外边框（左右黑色竖线 + 上下横线），如果单元格 `tcPr` 里没有**显式** `<a:lnL><a:noFill/>`、`<a:lnR><a:noFill/>`，外边框就会渲染出来。

- PowerPoint 原生创建的表格，每个单元格 `tcPr` 会写全 `lnL/lnR/lnT/lnB`（不需要的边用 `noFill` 显式关闭），所以不受默认样式影响。
- python-pptx 生成的表格只写需要的边（如 `lnB`），`lnL/lnR/lnT` 缺失，移出样式后就会被默认样式补上边框。

**修复**：给每个单元格补上显式声明——`lnL`、`lnR` 一律 `noFill`；表头行（r0）再加 `lnT noFill`（表头下方已有 1.5pt 黑线，不要再画上边框）。

**检查方法**：任何"移除了 styleId 的表格"，渲染后都要检查左右边缘是否有竖线。像素验证时，在表格左右边缘 ±1px 处统计非白像素，超过阈值（如 10 个）即为有边框。

**预防**：用 python-pptx 新建表格时，如果目标是无左右边框的极简横线表，直接写全四边声明（不需要的边用 `noFill`），不要依赖移除样式后的默认行为。

## 34.9 第 3 页双栏优化实战的坑（2026-08）

### 34.9.1 表格行高压缩有上限，不是想压多低就压多低

**现象**：第 3 页左列 `liab-table` 每行 0.31"（6 行共 1.83"），表格底部 7.57" 超出页面高度 7.5"，视觉贴底。

**根因**：0.31" 是内容（长文本 3 列）撑高的**实际高度**，不是默认值。python-pptx 读到的行高是真实渲染高度。

**关键**：压缩行高有下限——8pt 文字 2–3 行内容约需 0.26"，压到 0.24" 以下文字会被截断。

**修复**：压行高到 0.26"（下限附近）后，必须用 COM 读行高确认文字未截断（`Rows(i).Height` 若仍等于内容所需高度说明压不动）；若仍超界，则**上移表格**或**缩短文案**（改文案需用户确认）。压缩和上移是组合拳，不要只压一个。

**原则**：任何表格下方要放元素时，先量真实底部（top + 各行高之和），确认底部余量 ≥ 0.15" 再决定方案。

### 34.9.2 python-pptx 新建形状的段落默认对齐

**现象**：新建卡片（`add_shape`）后文字看起来居中，实际段落 `algn="ctr"`（居中）。

**修复**：显式设置段落 `p.alignment`（左对齐），或直接删除 `algn` 属性（默认即左对齐）。

**检查**：标题文字起点应贴近卡片左边距（实测偏移 0.15"–0.20" 属正常），不要凭「看着居中」判断——渲染后按像素验证。

### 34.9.3 卡片背景色：浅蓝 vs 浅灰

**现象**：右列三张浅蓝 #E3EBF4 卡片让页面「右边重、左边轻」。

**原则**：

- **需要强调的步骤**：浅蓝 #E3EBF4 + 深蓝顶条 #1F4E79（信息重要、需要一眼看到）
- **需要弱化的**：浅灰 #F0F0F0 + 保留小面积强调（深蓝顶条 + 蓝色标题）

**弱化 = 中性色大底 + 保留强调色细节**。大面积强调色会让该侧显重；需要减弱时换成中性色，但不要连强调细节一起删掉，否则页面失去重点。

### 34.9.4 复制页面后要改页序

**现象**：`add_slide` 复制页后新页出现在**最后**，不是目标位置。

**修复**：在 `sldIdLst` 中先 `remove` 再 `addnext` 到目标页后，页序才正确。

## 34.10 韦恩图改几何的坑（2026-08）

### 34.10.1 等大圆独有区相等，无法体现单侧更多

**现象**：用户要求「重叠大一点 + 蓝色独有区多一点」。等大圆时左独有区 = 右独有区 = 圆心距，无论圆心距怎么调，两侧独有区恒相等。

**根因**：等大圆独有区宽度 = 圆心距；重叠 = 直径 − 圆心距。二者此消彼长，但两侧之比恒为 1:1。

**修复**：不等大圆——蓝圆 2.70"、金圆 2.45"、圆心距 1.355" → 重叠 1.22"、蓝独有 1.48"、金独有 1.23"。

**检查方法**：几何公式先手算再写码：`重叠 = R1+R2−SEP`，`蓝独有 = 金圆左缘−蓝圆左缘`，`金独有 = 金圆右缘−蓝圆右缘`。

### 34.10.2 渲染验证时把页面标题误判为标签文字

**现象**：验证脚本检测「标签文字锚定错误」，多次分析发现是假警报——检测区域把上方页面标题（黑色文字）的像素段算进了标签文字。

**修复**：检测标签文字时限定 y 带（从标题文字底 `+0.02"` 开始），并按颜色区分（标签第一行深蓝/金色、第二行灰色 #606060，与黑色标题不同）。

**检查方法**：先定位所有文字行带（含标题），再按颜色逐行确认，不要只看位置。

### 34.10.3 spPr 元素顺序与 noAutofit（重复确认）

**现象**：半透明圆填充被忽略（回退不透明主题深蓝）、多段落文本框段落错位。

**根因**：`spPr` 子元素必须按 schema 顺序 `prstGeom → solidFill → ln → effectLst`；`python-pptx` 默认 `spAutoFit` 会使多段落文本框收缩。

**修复**：手动按 schema 顺序重建 fill/ln/effectLst；`bodyPr` 手动替换为 `<a:noAutofit/>`。

### 34.10.4 表格位置设置：`shape.top/left` 才有效，直接写 xfrm `off` 属性无效

**现象**：脚本里 `table._tbl.getparent().getparent().attrib['off'] = ...` 设置了表格位置，渲染后表格位置没变。

**根因**：`getparent().getparent()` 是 graphicFrame 元素，`off` 是 graphicFrame 内部 `<a:xfrm>` 子元素的属性，不是 graphicFrame 的属性，写在 graphicFrame 上无效；且 python-pptx 的 `Table` 对象没有 `.shape` 属性。

**修复**：`gf = table._graphic_frame; gf.top = Inches(y); gf.left = Inches(x)`。

**检查方法**：设置位置后读取 `shape.top` 确认生效，再渲染验证。

### 34.10.5 表格行高被内容撑高：先分清「指定的行高」与「内容的最小高度」

**现象**：数据行指定 0.12"，但渲染后被撑到约 0.145"，表格底部越界。

**根因**：表格行高是「指定高度」与「内容最小高度」取大者。内容最小高度由三部分决定：字号、行距（默认 1.2 倍）、单元格上下边距（默认 marT/marB 45720 EMU = 0.005"）。8pt 文字在默认行距和边距下的内容最小高度约 0.13"–0.145"，指定 0.115"–0.12" 会被撑高。

**修复**（三管齐下）：
1. `tcPr` 设 `marT="0" marB="0"`（默认是 45720 EMU）
2. 段落 `p.line_spacing = 0.9`（写 `<a:lnSpc><a:spcPct val="90000"/>`）
3. 行高至少 0.12"（8pt + 边距 0 + 行距 0.9 的下限）

**检查方法**：改完用 COM 读 `Rows(i).Height`（pt，÷72 得英寸）确认每行实际高度，再渲染验证底部。**COM 读到的行高就是 PowerPoint 最终渲染高度**，比像素检测更直接。

### 34.10.6 空单元格（`<a:t/>`）按主题默认字号算行高

**现象**：表头行指定 0.14"，但 COM 读到 0.27"（= 18pt × 1.2 倍行距 × 0.9）。

**根因**：表头行第二列是「空文本 run」（`<a:t/>` 空文本）。PowerPoint 对没有文字内容的 run 按**主题默认字号 18pt** 计算行高，而不是单元格内其他 run 的 8pt。8pt×1.2×0.9 ≈ 8.6pt，18pt×1.2×0.9 ≈ 19.4pt = 0.27"。

**修复**：空单元格放**空格字符**（`<a:t> </a:t>`），有实际文本的 run 才会按 8pt 计算行高。

**检查方法**：表头行高异常偏高（0.27" 而非设定的 0.14"）时，查该行是否有 `<a:t/>` 空文本 run。

---

### 34.11 复制整页为副本的坑（2026-08）

**现象**：需要「第 X 页的完整副本」时，手动重建形状容易漏、且不保留原始格式。

**修复**（已验证可行）：
1. `add_slide(src.slide_layout)` 新建页（用源页 layout），新页自带占位符要删掉（`list(slide.placeholders)` 逐个 `remove`）
2. 深拷贝源页 `spTree` 中的 `sp` / `graphicFrame` 子元素（`copy.deepcopy`）append 到新页 `spTree`
3. 用 `sldIdLst.remove + insert` 调整页序到目标位置（新页默认加在末尾，见 34.9.4）
4. 所有 `cNvPr` 的 `name` 加后缀（如 `-c`）避免与源页重名、便于单独编辑
5. 表格是 `graphicFrame` 不是图片，深拷贝即可完整复制；检查有无 `oleObj` / `pic`（图片、嵌入对象）才需要额外处理

**检查方法**：复制后逐形状对比（文字、位置、尺寸），再 COM 打开验证页数、无损坏。

### 34.12 圆角矩形：用 `MSO_SHAPE.ROUNDED_RECTANGLE`，不要手写 prstGeom（2026-08）

**现象**：`add_shape(RECTANGLE)` 后手写 `prstGeom.set('prst','roundRect')`，`spPr.find` 返回 `None` 抛异常。

**根因**：python-pptx 建出的形状 spPr 结构与我手动构造的查找路径不一致（重复确认 34.10.3 的 schema 顺序坑）。

**修复**：直接用 `MSO_SHAPE.ROUNDED_RECTANGLE` 建圆角矩形，`shape.adjustments[0] = 0.1` 调圆角大小。圆角标签条（pill）用它做，文字框垂直居中放进去。

### 34.13 画像类卡片：值主角 + 分组标题 + 假数据标注（2026-08）

**现象**：用户要求从「指标平铺」升级为「先看结论再看支撑」的客户画像布局。

**修复**（用户确认有效）：
- **值主角**：核心数值 12pt 粗深蓝 + 标签 8pt 深蓝（非粗），一改之前「标签 10pt 粗蓝 + 值 8pt 灰」——让第一眼看到数字结论
- **分组**：组标题（金色小竖条 + 10pt 粗深蓝文字）+ 组内指标行/短语行，替代全平铺
- **画像标签**：顶部 4 个浅金圆角 pill（#FAF3E0 底 + #D6A000 1pt 边 + 9pt 深蓝粗文字），一眼给出画像结论
- **生命周期 + 进度条**：借款→已还→待还 14pt 大数字 + 金灰进度条，展示贷款进展
- **True/False 自然语言化**：「✓ 多头借贷」短语形式（金色 ✓ 前缀 + 深蓝 9pt），不显示布尔值
- **假数据**：主文件无「违约次数」等数值时，按用户确认用自洽假数据（如 0 次，与「否」一致），汇报时明确标注

**检查方法**：核心信息逐项核对 + COM 折行/边界/重叠扫描。

## 34.14 画像类页面去重的经验（2026-08）

**现象**：画像页「顶部标签 + 分组指标」双通道展示时，同一信息出现 2 次以上（多头借贷/持续还款/SACC/当前余额 A$564 等），页面像指标清单而非客户画像。

**修复**（用户确认有效）：
- **顶部标签只留结论**，不重复下方指标；下方删掉与标签重复的分组（如「借贷特征」整组删除）
- **同一数字只出现一次**：顶部已有借款/已还/待还，下方删「当前余额」；顶部已有贷款类型/APR，下方不重复
- **分组名改为「动词+名词」职责型**（负债压力 / 还款与风险表现 / 还款计划 / 风险表现 / 产品与成本），各模块回答不同问题（整体负债如何 / 这笔贷款怎么借怎么还成本多高）
- **被删模块的独有指标并入相关分组**（「90 天还款 9 次」并入还款与风险表现），信息不丢失
- 指标行格式统一为「标签 8pt 深蓝 + 值 11pt 深灰」两行式，值字号升级（8→11pt）替代 ✓ 前缀，牺牲的视觉层级用字号补足

**检查方法**：改后全页搜索关键数字出现次数（A$564 只应在生命周期待还处出现 1 次），COM 验证形状完整、位置无重叠。

## 34.15 画像指标行：名称主角 > 数字（2026-08，推翻 34.13 的「值主角」）

**现象**：用户明确要求「指标的名称要大于数字，因为重点是指标，指标需要黑色，数值用灰色」——与 34.13「值主角」（值 12pt 粗蓝 + 标签 8pt 深蓝）相反。

**修复**（用户确认）：
- **指标行格式**：「名称 10pt 黑色加粗 + 数值 8pt 灰色 #606060（非粗）」**单行两段式**，名称字号 > 数字字号
- **数值的颜色与字号随页面层级**：一般指标数值 8pt 灰；风险类重点数值（轻微逾期 1 次 / 最长逾期 3 天）维持金色 12pt 粗体，不降为灰
- **顶部生命周期金额**（借款 A$1,500 / 已还 A$936 / 待还 A$564）是页内最大的数值主视觉，保持 14pt 深蓝粗体不受影响
- **删除还款进度条**：进度条（标签 + 百分比 + 底条 + 填充条 4 元素）整组删除，右侧指标区上移空出的 0.30" 高度，与左侧分组标题行（T≈4.92"）对齐，消除底部空白

**注意**：单行两段式需检查折行——名称 10pt + 数值 8pt 总宽超框即折行；左侧列宽由 1.31" 扩至 1.40"/1.60"，右侧 1.76" 无需扩。

**检查方法**：COM 逐 run 读 Font.Size/Color/Bold 确认「10pt 黑粗 + 8pt 灰」；Lines vs Paragraphs 数对比查折行；删除的进度条 4 元素确认不存在。

## 34.16 表格增删行：先验证「单元格内容折行决定行高」，再排布局（2026-08）

**现象**：第 4 页 Case 3 表格加 2 行后，COM 实测行高 0.31" 而非设定的 0.22"，表格整体高度 1.75"（预期 1.32"），结论条与表格重叠。

**根因**：表格行高 = max(设定行高, 内容最小高度)。「Non-SACC Loans（LOC 循环授信）」8pt 在 1.31" 宽列折成 2 行，内容撑高行——表格行高由**最宽单元格**决定，不是由平均单元格决定。

**修复**：
- **列宽由文本决定**：「Non-SACC Loans（LOC 循环授信）」8pt ≈ 1.77"，列宽 1.55" 仍折行，加到 1.80" 才单行（中文全角括号宽度不可低估）
- **精简长单元格文案**：「Non-SACC Loans（LOC 循环授信）」→「Non-SACC（LOC 循环授信）」，「Loans」与表头「我方分类」重复
- **改列宽时保持总宽不变**：col6 1.31→1.80（+0.49），col4 2.70→2.21（-0.49），总宽 7.66" 不变
- **重排顺序**：先改表格内容+列宽并验证行高，再移动周围形状（删行后上移、加行后下移），否则按错误高度算的偏移全部返工

**检查方法**：COM 读每格 `TextRange.Lines()` 数（>1 即折行）+ `Rows(i).Height`（真实渲染行高）；折行未消前不要动布局。

**其他注意**：Case 2 删行要保留「矛盾证据」——删中间重复行，保留放款 + SACC 还款 + Non-SACC 还款各 1 行，论点（前 SACC 后 Non-SACC）在 3 行内依然成立。

## 34.17 画像双栏「人 vs 贷」分工 + 栏内多模块布局（2026-08）

**现象**：第 2 页画像双栏原为「左侧黄标签 4 个 + 指标平铺（负债压力 / 还款与风险表现 2 组）」，右侧「金头 + 生命周期 + 三栏（还款计划 / 风险表现 / 产品与成本）」。用户反馈：左侧标签抢视觉且与下方重复、信息散；右侧三栏一长一短；左右两个大框 + 内部黄色框太多，有「报表感」。

**修复**（用户确认方向）：
- **左右分工**：左卡回答「这个人是什么样的人」（负债情况 / 履约表现 / 风险信号），右卡回答「这笔贷款现在是什么状态」（生命周期 + 还款计划 / 履约表现 / 产品成本）
- **标签弱化**：左侧 4 个浅金 pill 高度 0.28"→0.20"、字号 9→8pt，文案改结论式（多头借贷 / 近期新增贷款 / 高成本借贷 / 存在轻微逾期），不再与下方指标重复
- **栏内多模块**：左侧卡内改为 3 个横向模块（负债情况 / 履约表现 / 风险信号），每模块 1 行标题（10pt 深蓝粗）+ 2 行指标（名 10pt 黑粗 + 值 8pt 灰，名称主角，见 34.18；本节的 34.17 初版「名 9pt 灰 + 值 15pt 深蓝粗」已被推翻），第 3 行小字补充；模块间用 1pt 浅灰 #D6D6D6 细分隔线（高约 1.15"），替代整块黄框
- **风险强调色**：风险类核心数字（轻微逾期 1 次 / 扣款失败 1 次）用金色 8pt 粗体，与普通灰色数值区分（尺寸仍小于名称 10pt，颜色强调不受尺寸影响）
- **右侧头去框**：金色整框（Fa-head）删掉，改为无框一行「SACC ｜ 在贷」；APR 42% 从头部移入「产品成本」栏，仍金色
- **生命周期放大**：借款→已还→待还数字 14→18pt 深蓝粗（规范 5.2 一级数字上限），标签移到数字**下方**（借款金额 / 已还本金 / 剩余本金），成为全页核心视觉
- **三栏对齐**：右侧三栏统一 4 / 4 / 3 行，行高 0.24"，组标题 y 5.02"，不再一长一短
- **减少框线**：只保留左右两大白卡（35.0 白卡 + 浅灰描边），卡内用标题 + 细分隔线 + 数字层级建立结构

**注意**：栏内多模块的 x 坐标必须以**所在卡的实际边界**为基准计算，不能按全页坐标直觉排——本次左卡 3 栏误用全页 x（4.82 / 8.83）导致模块跑进右卡区域，改回卡内 x（0.809 / 2.749 / 4.689）后重叠为 0。

**检查方法**：COM 逐 run 读 Font.Size/Color/Bold 确认两段式；`Lines().Count` 确认无折行；按形状边界做两两重叠检测（排除卡背景和标签成对 bg+text）。

## 34.18 画像指标行「名称主角」回归（2026-08）

**现象**：34.17 将指标行做成「值主角」（名 9pt 灰 + 值 15pt 深蓝粗），用户查看成品后再次反转：**指标名称的字号必须大于数值，关键是指标名称**——数值离开名称无意义（如「预计待还 A$1,016」，「预计待还」才是要传达的信息）。

**结论**（用户确认，已落地第 2 页全部 19 个指标行）：
- 名称 10pt 黑粗 #0A0A0A > 数值 8pt 灰 #606060 不加粗（与 34.15 完全一致）
- 风险类数值（轻微逾期 / 扣款失败 / 最长逾期 / APR）保持金色 #D6A000 8pt 粗体——颜色强调与字号大小解耦，尺寸仍小于名称
- 模块标题（10pt 深蓝粗）与行名称（10pt 黑粗）同号，靠颜色区分层级

**反转史**：同一「指标行谁是主角」组件 34.13 值主角 → 34.15 名称主角 → 34.17 值主角 → 34.18 名称主角，用户已两次确认名称主角（34.15 / 34.18），后续**默认名称主角**，除非用户原话明确要求值大。

**注意**：PowerPoint 保存时会合并相邻同格式 run（如「贷款机构」+「2 家」同为 9pt 灰无粗 → 并成单 run），改两段式前先逐 run 检查；单 run 行需按 名称/数值 边界拆 run 再分别设格式。

**检查方法**：python-pptx 逐 run 读 Size/Bold/Color 确认 名称 10pt 黑粗 + 数值 8pt 灰/金 两段式；拆分后文字拼接与原始文本一致（不增删字符）。

## 34.19 批量新增指标行的三个坑（2026-08）

**现象**：第 2 页右卡补充负债画像指标（三列各加 1 行 + 底部新模块 6 格）时，连续踩三个脚本级错误：①第 4 行 y 偏移只加了 1 个行距（ROW_STEP 237744）而非 3 个，新行叠在第 2 行上；②`if val_gold is not None:` 判断使 `gold=False` 也进入金色分支，普通指标行全部变金；③`a:br` 换行符不带 rPr 时 COM 读到 18pt 默认字号，撑大第二行行高。

**根因**：脚本复用模板形状（deepcopy）批量改文字时，坐标与格式的「相对计算」没有打印核对就保存；布尔参数用 `is not None` 而非真值判断；新建换行元素未继承相邻 run 的 rPr。

**修复**：
- 行偏移 = 行数 × ROW_STEP（从第 1 行到第 N 行加 N−1 个行距），保存前打印新行 y 与预期值比对（脚本输出「底部模块标题 y=5.63"」式核对）
- 布尔分支一律 `if val_gold:`；参数默认 False 不显式传 None
- `a:br` 元素必须 `br.append(deepcopy(值 run 的 a:rPr))`，否则 PowerPoint 按 18pt 默认字号渲染换行、行高异常
- 新增行用深拷贝现有「两段式」模板（灰值复制 用户-m1-2、金值复制 用户-m3-1），格式自动继承，只改 cNvPr id/name、xfrm off、a:t 文本

**检查方法**：COM 逐 run 读 Font.Size/Color 区分灰（#606060 非粗）/金（#D6A000 粗）；`Lines().Count` 查意外折行；两两重叠检测只看新增形状（相邻列的「框边界重叠但文字不重叠」是合法现象，勿误报）。


# 35. 步骤卡片组件（浅灰卡片）

## 35.0 白色描边卡（白卡 + 浅灰描边）

适用于：

- 双栏页中需要容纳 KPI + 键值表的**区域容器**
- 原浅蓝整卡改白后，弱化"下方太重"的视觉重心

参数：

| 项目 | 参数 |
|---|---|
| 底色 | 纯白 #FFFFFF |
| 描边 | 1.5pt 浅灰 #D6D6D6（白底上可见，需 ≥ 1pt；0.75pt 太浅） |
| 顶部深蓝条 | 0.06" #1F4E79（保留强调色细节） |
| 卡内表格 | 键值表行分组：浅蓝表头行 #E3EBF4 + 白数据行 + 值列深蓝加粗 |

原则：

- 白卡 = 中性大底 + 深蓝顶条 + 浅蓝表头行呼应，视觉重量远低于浅蓝整卡
- 卡内表格行高压缩规则见 34.10.5 / 34.10.6（边距 0 + 行距 0.9 + 空单元格放空格）

## 35.1 适用场景

- 步骤说明（3–4 个，如「监控 → 定位 → 输出」）
- 流程要点
- 能力模块要点

不适合：

- 精确数字（用表格）
- 一句话结论（用结论条）
- 超过 5 个步骤（改表格或流程条）

## 35.2 参数

| 项目 | 参数 |
|---|---|
| 宽度 | 随所在栏宽（半栏约 6.06"） |
| 高度 | 0.55"–0.60" |
| 垂直间距 | 0.08"–0.10" |
| 顶部深蓝条 | 0.06" #1F4E79 |
| 底色 | #F0F0F0（默认弱化）或 #E3EBF4（需要强调时） |
| 内边距 | 左 0.14"、右 0.08"、上 0.05"、下 0.04" |
| 标题 | 11pt 加粗 #1F4E79，左对齐 |
| 正文 | 8.5pt 常规 #3D3C3A |

## 35.3 结构

标题与正文放在**同一个文本框**内（标题在上、正文在下），不要拆成两个独立文本框。

## 35.4 原则

- 标题一行、正文最多两行（超长拆内容或改表格）
- 卡片文字全部**左对齐**（不要居中）
- 一列卡片宽一致、左边对齐、顶部深蓝条对齐
- 3–4 个最合适；与同页其他蓝色元素（表头、胶囊、底带）同色系呼应，形成视觉平衡

---

# 36. AI 修改前布局风险自检清单

AI 修改 PPT 页面时，改前改后都按此清单自查。

## 改前

- [ ] 备份原文件
- [ ] 量清所有相关元素的**真实位置**（top / 行高 / 底部），不用记忆值

## 布局风险

- [ ] 表格行高是否被内容撑高？真实底部是否 > 7.45"（页面 7.5" 至少留 0.05"）？
- [ ] 压缩行高是否低于下限（8pt 文字 2–3 行内容 ≥ 0.26"）？压不动就上移表格或缩短文案（需用户确认）
- [ ] 左右列颜色重量是否平衡？一侧有大面积色块时，另一侧是否有同色系呼应（表头 / 胶囊 / 底带）？不对称时优先「小面积呼应」而非「另一侧加更大色块」
- [ ] 新建形状的段落对齐是否意外居中（`algn="ctr"`）？标题文字起点应贴近左边距
- [ ] 新增元素是否与既有元素重叠（量真实边界，不要目测）？

## 改后

- [ ] 用 COM 读结构化属性验证（行高、折行、颜色/字号），确认文字未截断、未溢出、未重叠
- [ ] 若动过 layout，检查所有页无溢出、无内容缺失

---

# 37. 双栏视觉平衡指南

## 37.1 为什么需要平衡

双栏页（左证据 + 右解释）中，若一侧集中了大量色块（卡片、底带、统计卡），另一侧只有文字和细线表格，会形成「视觉重心偏移」——阅读时感觉页面向一侧倾倒，该侧显得比另一侧重要，但内容本身没有主次关系。

**视觉重量 ≠ 信息重要性**。页面要平衡，不是把真正重要的内容压轻。

## 37.2 平衡方法（按优先级）

1. **先减重，不加重**：优先减弱重侧的色块（换中性浅灰、缩小面积），而不是在轻侧加更大的色块对冲。加重会提升页面噪声，减重才回归简洁。
2. **同色系小面积呼应**：轻侧需要呼应时，用与重侧**相同颜色**的小面积元素（浅蓝表头、胶囊、细条），不要用新颜色。
3. **对称布局**：重侧 n 个色块，轻侧 n 处同色系呼应（不一定要面积相等，1:1 数量即可让眼睛平衡）。
4. **中性色替代**：重侧大面积强调色块（如浅蓝整卡）可改为中性色（浅灰 #F0F0F0），保留强调细节（深蓝顶条 + 蓝色标题），视觉重量立刻下降。

## 37.3 面积参考

- 大面积色块（>0.4" 高 × 半栏宽）：视觉重量 ≈ 3 个表头 + 1 个结论条
- 浅蓝表头（0.17" 高）：轻量呼应，3 个约抵 1 个卡片
- 单侧色块数量上限：2–3 个，超过即考虑减重或改表格

## 37.4 检查方法

改完用 COM 读左右两列形状的填充色与面积（`Fill.ForeColor.RGB` + 尺寸），统计浅蓝色总面积，若一侧 > 另一侧 2 倍，通常需要调整；差异 < 1.5 倍视为平衡。

## 37.5 注意

- 平衡是「视觉」不是「对称」——数据页左侧表格多、右侧卡片多很正常，只要颜色重量均衡即可
- 不要为了平衡把原本强调的重点强行弱化（如把核心 KPI 卡片改浅灰）
- 深浅色（深蓝 vs 浅蓝）重量不同：深蓝顶条 0.06" 的重量 ≈ 浅蓝 0.3" 色块，计重时按「深色 > 浅色」
- 用户原话（2026-08 第 2 页）：「头轻脚重，下面的色块太多了」——下半部色块多时，优先把整卡改为白卡 + 浅灰描边（保留深蓝顶条），而不是在上半部加色块对冲（见 35.0 白色描边卡）

---

# 38. 页面标题基准参数（2026-08 全篇统一）

全篇 4 页主标题、副标题已统一为第 1 页基准，参数如下：

## 38.1 主标题

| 参数 | 值 |
|---|---|
| 位置 | (0.66, 0.23)" |
| 尺寸 | 9.72 × 0.39" |
| 字号 | 20pt 加粗 |
| 颜色 | #0A0A0A |
| 字体 | 微软雅黑（含 ea 子字体） |
| 行距 | 130% |
| wrap | none |

## 38.2 副标题

| 参数 | 值 |
|---|---|
| 位置 | (0.56, 0.64)" |
| 尺寸 | 11.92 × 0.48" |
| 字号 | 18pt |
| 颜色 | #606060 |
| 字体 | 微软雅黑 |
| 行距 | 140% |

## 38.3 注意事项

- 副标题 noAutofit（或默认）下，2 行（每行 18pt × 1.4 = 0.35"）共 0.70" 超出框高 0.48"，文字会溢出到框外与下方元素视觉重叠——但 PowerPoint 只渲染不裁剪，溢出部分可读；**副标题必须控制在单行**（≈ 47 个中文字符以内）
- 验证「副标题是否溢出」时，只统计框内 (0.56–12.48, 0.64–1.12)" 区域，且注意下方小节标题（TextBox 23 等 12pt 文字，位于 y 1.15" 起）可能被误判为副标题第二行——按 y 带 + 内容归属确认
